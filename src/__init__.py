"""minicourse_multimodal: model-architecture code for the multimodal survival-prediction minicourse.

Tutorial logic (data prep, training loops, evaluation) lives directly in the notebooks.
This package only holds things that can't reasonably be pasted into a notebook — e.g.
large third-party model architectures reused unchanged across multiple notebooks.
Currently empty; populated starting with the feature-extraction notebook.
"""
